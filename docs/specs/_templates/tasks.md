# <Feature name> — Tasks

<!--
Copy this file to docs/specs/<feature-name>/tasks.md.
Write it only after design.md is approved.

Rules:
- Coding steps only. No "research", "discuss" or "deploy" items.
- Each task cites the requirement numbers it implements, e.g. "_Requirements: 1.2, 1.3_".
- Each task is small enough to finish and test in one sitting, and leaves the
  solution building.
- Order tasks so that each builds on the previous one — domain, then persistence,
  then application, then API, then tests.
- Check a box only when the code exists and `dotnet test SmartAppointments.slnx`
  passes. Check it in the same commit as the code.
-->

- [ ] 1. <Task>
  - <Sub-step>
  - <Sub-step>
  - _Requirements: 1.1, 1.2_

- [ ] 2. <Task>
  - _Requirements: 2.1_

- [ ] 2.1 <Sub-task, when a task is large enough to split>
  - _Requirements: 2.1_
